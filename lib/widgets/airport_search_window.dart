import 'package:flutter/material.dart';

class AirportSearchWindow extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white.withOpacity(0.7),
      body: Container(),
    );
  }
}
